# server/content/adapters — auto-discovered adapter sub-packages.
# Each sub-package must expose an ``adapter.py`` module with an ``ADAPTER``
# instance (and optionally ``ADAPTER_CLASS``) for registry auto-discovery.
